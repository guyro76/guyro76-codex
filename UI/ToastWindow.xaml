<Window x:Class="LangFlipDesktop.UI.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        Topmost="True"
        SizeToContent="WidthAndHeight"
        FlowDirection="RightToLeft">
    <Border Background="#F0101A2E" BorderBrush="#38D5FF" BorderThickness="1" CornerRadius="10" Padding="18,12">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="2" Opacity="0.6" />
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Ellipse Width="10" Height="10" Fill="#38D5FF" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBlock x:Name="MessageText" Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold"
                       MaxWidth="360" TextWrapping="Wrap" VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</Window>
